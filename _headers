# Cloudflare Pages: games.imapp.shop
# imapp (https://imapp.shop) から iframe で埋め込まれるゲーム配信。

# ---------- burger (Burger Stack Rush / Phaser+Vite版) ----------
# imapp 配下からのみ iframe 許可。clickjacking 対策。
# 追加で許可したいプレビュー環境があればここに足す。
/burger/*
  Content-Security-Policy: frame-ancestors 'self' https://imapp.shop https://*.imapp.shop https://imapp.pages.dev https://*.imapp.pages.dev http://localhost:3000 http://127.0.0.1:3000

# Vite ビルド。差し替え事故防止を最優先し index.html と素材は常に再検証する。
# （JS は内容ハッシュ名なので本来長期キャッシュ可だが、確実な更新反映を優先）
/burger/index.html
  Cache-Control: no-cache, must-revalidate
/burger/assets/*
  Cache-Control: no-cache, must-revalidate

# ---------- packer-panic (ふんべつ！パッカーパニック) ----------
# 別開発の Web ゲーム (天使カーソル制) を iframe 埋め込み。
# 同じく imapp 配下からのみフレーム許可。
/packer-panic/*
  Content-Security-Policy: frame-ancestors 'self' https://imapp.shop https://*.imapp.shop https://imapp.pages.dev https://*.imapp.pages.dev http://localhost:3000 http://127.0.0.1:3000

# index.html は内容ハッシュなしで差し替わる → 常に最新化
/packer-panic/index.html
  Cache-Control: no-cache, must-revalidate
# 画像など同名で差し替わり得る素材があるため、確実な反映を優先して常に再検証。
# （JS は内容ハッシュ名なので 304 で済み、実害は小さい）
/packer-panic/assets/*
  Cache-Control: no-cache, must-revalidate

# ---------- tsukurun (ツクルン / 家具組み立てタイムアタック) ----------
# Phaser4 + Vite 製ゲーム (つくチャレ-phaser) を iframe 埋め込み。
# 同じく imapp 配下からのみフレーム許可。
/tsukurun/*
  Content-Security-Policy: frame-ancestors 'self' https://imapp.shop https://*.imapp.shop https://imapp.pages.dev https://*.imapp.pages.dev http://localhost:3000 http://127.0.0.1:3000

# index.html はビルドごとに JS ハッシュ参照が変わる → 常に最新化
/tsukurun/index.html
  Cache-Control: no-cache, must-revalidate
# Vite のハッシュ付きアセット (assets/index-XXXX.js) は内容不変 → 長期キャッシュ
/tsukurun/assets/*
  Cache-Control: public, max-age=31536000, immutable
# ドット絵画像は同名で差し替わり得るので再検証（後勝ちで上書き）
/tsukurun/assets/pixel/*
  Cache-Control: no-cache, must-revalidate

# ---------- pizza-oven-rush (ピザ・オーブン・ラッシュ) ----------
# Phaser4 + Vite 製ゲーム (pizza-oven-rush) を iframe 埋め込み。
# 同じく imapp 配下からのみフレーム許可。
/pizza-oven-rush/*
  Content-Security-Policy: frame-ancestors 'self' https://imapp.shop https://*.imapp.shop https://imapp.pages.dev https://*.imapp.pages.dev http://localhost:3000 http://127.0.0.1:3000

# index.html はビルドごとに JS ハッシュ参照が変わる → 常に最新化
/pizza-oven-rush/index.html
  Cache-Control: no-cache, must-revalidate
# 具材画像など同名で差し替わり得る素材があるため、確実な反映を優先して常に再検証。
# （JS は内容ハッシュ名なので 304 で済み、実害は小さい）
/pizza-oven-rush/assets/*
  Cache-Control: no-cache, must-revalidate

# ---------- apple-guard (アップルガード 〜りんごの木をまもれ〜) ----------
# Phaser4 + Vite 製ゲーム (apple-guard) を iframe 埋め込み。
# 同じく imapp 配下からのみフレーム許可。
/apple-guard/*
  Content-Security-Policy: frame-ancestors 'self' https://imapp.shop https://*.imapp.shop https://imapp.pages.dev https://*.imapp.pages.dev http://localhost:3000 http://127.0.0.1:3000

# index.html はビルドごとに JS ハッシュ参照が変わる → 常に最新化
/apple-guard/index.html
  Cache-Control: no-cache, must-revalidate
# りんご/サル/背景など同名で差し替わり得る素材があるため、確実な反映を優先して常に再検証。
# （JS は内容ハッシュ名なので 304 で済み、実害は小さい）
/apple-guard/assets/*
  Cache-Control: no-cache, must-revalidate

# 共通: Pages 自体のルートはまだ未使用 (将来ゲーム一覧 LP を置くかもしれない)
/*
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
